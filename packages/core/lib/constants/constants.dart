class Language {
  static const TH = 'th';
  static const EN = 'en';
}

enum FeedbackState {
  info,
  negative,
  warning,
  positive,
}

enum AppWidgetState{
  normal,
  empty,
  hovered,
  focused,
  pressed,
  dragged,
  selected,
  scrolledUnder,
  disabled,
  success,
  fail,
}
